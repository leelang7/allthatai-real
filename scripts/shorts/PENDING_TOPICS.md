# 우선 발행 대기 주제

사용자가 직접 지정한 주제다. **다음 쿼터에서 반드시 먼저 발행한다.**
발행에 성공하면 이 목록에서 지우고, 해당 JSON은 그대로 두면 된다.

| JSON | 주제 | 지정일 | 상태 |
|---|---|---|---|
| _(없음)_ | | | |

※ 이 파일에 있는 JSON은 "이전 날짜 JSON 재사용 금지" 규칙의 예외다.
   아직 한 번도 발행되지 않은 대기분이므로 그대로 발행하면 된다.
   (발행 전 `channel_titles.txt` 중복 확인은 동일하게 수행할 것)
